import React from 'react';

const Title = React.createClass({
  render() {
    return (
      <div>
        <section id="header">
          <header class="major">
            <h1>Purify CSS</h1>
              <p>Remove dead weight CSS from your applications</p>
          </header>
        </section>
      </div>
    );
  }
});

export default Title;